package com.venueninja.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WebConfig {
    // This class can be used for future MVC configuration if needed
    // CORS configuration is handled by SecurityConfig.java when Spring Security is present
}
